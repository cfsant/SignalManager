@typeparam TModel
@inherits Base

@this.Body

@code {
    [Parameter]
    public TModel Model
    {
        get;
        set;
    }

    [Parameter]
    public Action<TModel> OnEditCallback
    {
        get;
        set;
    }

    [Parameter]
    public Action<TModel> OnRemoveCallback
    {
        get;
        set;
    }

    [Parameter]
    public Action<TModel> OnModelUpdate
    {
        get;
        set;
    }

    [Parameter]
    public Action<TModel> OnModalCancelation
    {
        get;
        set;
    }

    protected override Task OnInitializedAsync()
    {
        return Task.Run(() => {
            this.Body = this.RenderBody();
        });
    }
}
